# P300 BCI application package.
#
# Application (application.py) is the shared skeleton. CalibrationMode and
# LiveMode subclass it and fill in the mode-specific hooks. The main_*.py
# launchers at the repo root call run_main with the mode they want.